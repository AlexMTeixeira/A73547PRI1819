extends layout.pug
block content
    .w3-container
        .w3-card-4
            header.w3-container.w3-teal
                h2 ThesisApp
                h4 Aplicação para Publicar teses
            .w3-container
                p Esta aplicação, no ambito a cadeira de PRI tem como objetivo registar e visualizar teses.
                hr
                table.w3-table.w3-centered
                    tr
                        th
                            form(action='/registo', method='GET')
                                input.w3-btn.w3-blue-grey(type='submit', value='Registar')
                        th  
                            form(action='/lista', method='GET')
                                input.w3-btn.w3-blue-grey(type='submit', value='Consultar')
                br
            footer.w3-container.w3-teal
                address Gerado por thesisApp